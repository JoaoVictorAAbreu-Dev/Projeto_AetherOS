# User ABI

This directory tracks userland-facing ABI notes and future headers or bindings derived from the Rust ABI crate.

The current post-v1 foundation now assumes a future ABI will need:

- syscall numbering discipline
- program image metadata
- execution-mode aware process definitions
