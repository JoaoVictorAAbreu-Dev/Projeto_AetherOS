# Fixtures

Shared validation fixtures used by host checks and QEMU documentation live here.

Current fixtures:

- `qemu-boot-markers.txt`: expected boot-stage and success markers for headless boot validation
