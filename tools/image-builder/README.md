# Image Builder

This directory is intentionally available for optional ISO or disk-image assembly helpers beyond the current `xtask stage` ESP workflow.

Current v1 status:

- not required for the supported tester flow
- kept separate to avoid mixing image-packaging concerns into the kernel crate
