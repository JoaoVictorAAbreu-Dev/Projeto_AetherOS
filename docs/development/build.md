# Build

The repository uses a Cargo workspace layout. The current boot milestone wires the first real kernel entry path, but local execution still depends on installing Rust, Limine assets, and QEMU on the development machine.
