# Limine Protocol

Limine is the selected boot protocol for the early stages of AetherOS because it reduces bootstrap complexity and keeps the initial focus on kernel architecture instead of firmware bring-up.
