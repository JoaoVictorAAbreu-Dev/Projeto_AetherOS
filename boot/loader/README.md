# Boot Loader Notes

The initial boot strategy is based on the Limine boot protocol.

This directory is reserved for boot assets and local integration notes that are not part of the kernel crate itself.
